{
  "name": "reset",
  "displayName": "Reset",
  "description": "resets the code on visual studio to a default prompt (using for cp)",
  "version": "0.0.1",
  "publisher": "HarshitSoni",
  "engines": {
    "vscode": "^1.91.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./extension.js",
  "icon": "Images/icon.png",
  "contributes": {
    "commands": [{
      "command": "extension.reset",
      "title": "reset the vscode window to default scaffold"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.91.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}
